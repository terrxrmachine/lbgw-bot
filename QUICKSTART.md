# 🚀 Быстрый старт LBGW Telegram Bot

## За 3 минуты

### 1. Установка зависимостей (1 мин)

```bash
cd /Users/terrxrmachine/Documents/Работа/lbgw-bot
npm install
```

### 2. Проверка .env (30 сек)

Файл `.env` уже создан с базовыми настройками. Проверьте значения:

```bash
cat .env
```

Должно быть:
```env
TELEGRAM_BOT_TOKEN=7616206547:AAGZX9Uu4As9UdM3Lth2dVE7J-cfMaffvS4
TELEGRAM_CHAT_ID=6626945924
LBGW_API_URL=http://localhost:3000
REVIEWS_PUBLISH_API_KEY=lbgw_publish_reviews_secure_key_2025
```

### 3. Запуск (30 сек)

```bash
npm run dev
```

Вы должны увидеть:
```
[TIMESTAMP] [LBGW-BOT] [SUCCESS] ✅ LBGW Bot started successfully!
```

### 4. Тест (1 мин)

Откройте Telegram и напишите боту:
```
/start
```

Получите приветствие? ✅ Бот работает!

---

## Проверка функций

### ✅ Команды работают

```bash
# В Telegram:
/start    # Приветствие
/help     # Справка
/stats    # Статистика (требует настройки YM)
```

### ✅ Модерация отзывов работает

1. Откройте сайт http://localhost:3000
2. Оставьте тестовый отзыв
3. В Telegram придёт уведомление с кнопками
4. Нажмите ✅ Approve или ❌ Reject

### ⚠️ Статистика (требует настройки)

Для работы `/stats` нужны:
- `YM_COUNTER_ID` 
- `YM_OAUTH_TOKEN`

См. [SETUP.md](SETUP.md) для получения токена.

---

## Структура проекта

```
lbgw-bot/
├── src/
│   ├── config/          # Конфигурация
│   │   └── index.ts
│   ├── handlers/        # Обработчики
│   │   ├── commands.ts  # /start, /help, /stats
│   │   └── reviews.ts   # Модерация отзывов
│   ├── services/        # Внешние API
│   │   ├── lbgw-api.ts      # API сайта
│   │   └── yandex-metrica.ts # Яндекс.Метрика
│   ├── types/           # TypeScript типы
│   ├── utils/           # Утилиты
│   │   └── logger.ts
│   └── index.ts         # Точка входа
├── .env                 # Переменные окружения ✅
├── README.md            # Основная документация
├── SETUP.md             # Детальная настройка
├── COMMANDS.md          # Справка по командам
└── package.json         # Зависимости
```

---

## Решение проблем

### Бот не запускается

```bash
# Проверьте .env
cat .env

# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

### TypeScript ошибки

Это нормально до `npm install`. После установки зависимостей ошибки исчезнут.

### Отзывы не приходят

1. Убедитесь, что сайт работает: http://localhost:3000
2. Проверьте, что API ключ совпадает в `.env` бота и сайта
3. Посмотрите логи бота в консоли

---

## Следующие шаги

1. **Настроить Яндекс.Метрику** — см. [SETUP.md](SETUP.md)
2. **Создать канал для отзывов** — добавьте `TELEGRAM_REVIEWS_CHANNEL` в `.env`
3. **Production деплой** — используйте PM2 (см. [SETUP.md](SETUP.md))

---

## Полезные команды

```bash
# Development с hot-reload
npm run dev

# Production build
npm run build
npm start

# Проверка кода
npm run lint

# Просмотр логов (если PM2)
pm2 logs lbgw-bot
```

---

## Документация

- 📖 [README.md](README.md) — Основная документация
- ⚙️ [SETUP.md](SETUP.md) — Детальная настройка
- 💬 [COMMANDS.md](COMMANDS.md) — Справка по командам

---

## Поддержка

Проблемы? Проверьте:
1. Логи в консоли
2. Правильность токенов в `.env`
3. Доступность сайта LBGW
4. [SETUP.md](SETUP.md) для детальной настройки
